custom:
  kafka:
    consumer:
      bootstrap-servers: localhost:29092
      group-id: group_id

server:
  port: 5000

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/payments
    username: tech
    password: test
